#include "RightHandRuleMazeSolver.h"

RightHandRuleMazeSolver::RightHandRuleMazeSolver()
    : Micromouse() {}
    // , distanceSensor(std::make_unique<DistanceSensor>(maze, startX, startY)) {}

void RightHandRuleMazeSolver::updateSensors() {
    // distanceSensor->updateReadings(posX, posY);
}

int RightHandRuleMazeSolver::makeDecision() {
    // Implementacja algorytmu "prawej ręki"
        // Implement right-hand rule algorithm
    // // After deciding the next move, log the movement
    // int nextX = x; // Calculate next X based on the algorithm
    // int nextY = y; // Calculate next Y based on the algorithm

    // logMovement(nextX, nextY);
    // x = nextX;
    // y = nextY;
}
